
pub fn eval(input: &String) -> () {

}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn number_integer_literal_returns_value() {
        let result = eval(&String::from("4"));
        assert_eq!(false);
    }
}